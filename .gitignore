node_modules/
.expo/
dist/
npm-debug.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
# macOS
.DS_Store
# Temporary files created by Metro to check the health of the file watcher
.metro-health-check*

# Banco de dados local gerado pelo servidor (npm run serve)
data/

# Chave da IA (Nano Banana / Gemini) — NUNCA subir para o GitHub
.env
.env.local
*.env
!.env.example
