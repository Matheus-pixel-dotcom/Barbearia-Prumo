/*
 * Configuração do site — Style Relo Barber
 *
 * Endereço do servidor que guarda o banco de clientes.
 *
 *   ''  (vazio)  → o site usa o servidor do próprio endereço onde ele está aberto.
 *                  É o que você quer quando o site inteiro roda junto com o Node
 *                  (`npm run serve`, Render, Railway, Docker...).
 *
 *   'https://seu-endereco.onrender.com'
 *                → use quando o site está no GitHub Pages (hospedagem estática)
 *                  e o banco compartilhado está em outro servidor. Basta colar
 *                  aqui o endereço do servidor Node publicado.
 *
 * Exemplo:
 *   window.RELO_API_BASE = 'https://style-relo-barber.onrender.com';
 */
window.RELO_API_BASE = '';
